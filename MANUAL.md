# 📖 Gemini 이미지 수집기 V2.0 공식 매뉴얼

안녕하세요! 『Gemini 이미지 수집기 V2.0』을 구매 및 이용해 주셔서 감사합니다.
본 프로그램은 강력한 우회 수집 기능과 사용자 편의성을 갖춘 상업용 크롤링 툴입니다.

---

## 🚀 1. 빠른 시작 가이드

1. **프로그램 실행:** 받으신 ZIP 파일을 압축 해제한 뒤, 폴더 안의 `Gemini_Image_Crawler_v버전.exe` 를 더블 클릭하세요.
   * 폴더에 함께 들어 있는 **`바로가기_만들기.bat`** 을 실행하면 바탕화면에 바로가기가 자동으로 생성됩니다. (권장)
2. **이용약관 동의 & 라이센스 인증:** 첫 실행 시 이용약관 동의 창이 뜨고, 이어서 라이센스 키 입력 창이 나타납니다. 구매 시 발급받은 키를 입력해 주세요. (온라인 인증 방식이라 최초 1회는 인터넷 연결이 필요하며, 이후에는 만료일까지 오프라인에서도 사용할 수 있습니다.)
   * *라이센스는 **1 PC 1 키** 정책입니다. 다른 PC에서 쓰시려면 초기화가 필요하니 문의해 주세요.*
   * *기간이 만료되었거나 갱신이 필요할 때는 메인 화면 좌측의 [라이센스 갱신 / 연장] 버튼을 눌러 언제든 연장할 수 있습니다.*
3. **주소 입력:** 상단의 [수집할 주소(URL)] 칸에 이미지를 추출하고 싶은 대상 웹페이지 주소를 넣습니다.
   * `https://` 를 빼고 `example.com` 처럼 넣어도 자동으로 붙여줍니다.
4. **수집 시작:** `[🚀 수집 시작]` 버튼을 누르면 프로그램이 동작하며 이미지를 긁어옵니다.
   * *수집된 모든 결과는 프로그램 폴더의 `results/` 안에 엑셀 파일(`checklist.xlsx`)과 함께 정리되어 저장됩니다.*
   * *프로그램을 쓰기 권한이 없는 위치(예: `Program Files`)에 두면 결과·설정·로그가 자동으로 사용자 폴더로 대체 저장됩니다.*

> 💡 **설정은 자동으로 저장됩니다.** 값을 바꾸면 즉시 기억되고, 다음 실행에도 그대로 유지됩니다.
> 만약 저장에 실패하면 화면 로그에 사유와 해결 방법이 표시됩니다.

---

## 🛠️ 2. 핵심 상용화 기능 (PRO Features)

화면 중앙의 **[설정 탭]** 세 개에서 모든 기능을 조절합니다.
세 탭은 모두 `항목 이름 → 조절 부분` 이 같은 위치에 놓인 동일한 구조입니다.

> 💡 **회색으로 흐려진 입력칸은 "지금은 사용되지 않는다"는 뜻입니다.**
> 예를 들어 [특정 영역만] 체크를 하지 않으면 그 옆 입력칸이 흐려집니다. 체크하면 바로 사용할 수 있게 됩니다.
> (입력칸이 갑자기 나타나거나 사라지지 않으므로 화면이 흔들리지 않습니다.)

### 📌 [일반 설정] 탭

| 항목 | 설명 |
|---|---|
| **브라우저** | `화면 숨기기(Headless)` — 체크하면 크롬 창이 보이지 않게 조용히 작동합니다. 빠르지만 일부 사이트는 봇으로 감지할 수 있습니다. |
| **안전 딜레이** | 요청 사이의 대기 시간을 1~5단계로 조절합니다. 슬라이더 옆에 **`2단계 · 요청 간격 1.0~2.0초`** 처럼 실제 적용되는 초가 표시됩니다. 차단되거나 오류가 잦으면 단계를 올려주세요. |
| **수집 정책** | `robots.txt 준수` (기본 켜짐·권장) — 사이트가 "자동 수집을 원하지 않는다"고 밝힌 경우 수집을 중단합니다. **해제 시 모든 법적 책임은 사용자 본인에게 있습니다.**<br>`이어받기(중복 제외)` — 이미 받은 이미지는 건너뜁니다. 사이트별로 따로 기록됩니다. |
| **수집 범위** | `특정 영역만` — 페이지 전체가 아니라 특정 영역의 이미지만 뽑고 싶을 때 체크하고, 개발자 도구(F12)로 확인한 CSS 선택자(예: `#content`, `.gallery-grid`)를 넣으세요. |

> 🔄 **같은 사이트를 처음부터 다시 받고 싶을 때:** `이어받기(중복 제외)` 체크를 **해제**하고 수집하세요.
> 그러면 이미 받은 이미지도 다시 내려받습니다. (다시 체크하면 원래대로 중복을 건너뜁니다.)

### 📌 [필터 및 고급] 탭

| 항목 | 설명 |
|---|---|
| **최소 크기** | `가로 × 세로` px 이상만 수집합니다. 작은 아이콘·버튼·추적 픽셀을 걸러냅니다. 수집량이 너무 적으면 이 값을 낮춰보세요. |
| **허용 확장자** | `JPG / PNG / WEBP / GIF` 중 받을 형식만 체크합니다. **주소에 확장자가 없는 이미지도 실제 파일 형식을 확인해서 걸러냅니다.**<br>⚠️ 모두 해제하면 아무것도 수집되지 않습니다(허용 목록이 비어 있다는 뜻). |
| **제외 키워드** | 예: `logo, icon, banner` — 이미지 주소에 이 단어가 있으면 건너뜁니다. (사이트 도메인 이름에는 적용되지 않으므로 `bannershop.co.kr` 같은 사이트가 통째로 걸러지는 일은 없습니다.) |
| **필수 포함** | 예: `풍경, 아이폰` — 이미지 설명이나 주변 문단에 이 단어가 있는 이미지만 수집합니다. 비워두면 모두 수집합니다. |

### 📌 [접속 및 순회] 탭

| 항목 | 설명 |
|---|---|
| **수동 로그인** | 비공개 게시글·회원 전용 이미지를 받을 때 체크하세요. 브라우저가 열린 뒤 설정한 초(기본 30초) 동안 기다립니다. 그 사이에 직접 로그인하시면, **로그인 정보(쿠키)를 이미지 다운로드에도 그대로 사용**하므로 회원 전용 이미지까지 받을 수 있습니다.<br>*이 옵션을 켜면 화면을 봐야 로그인할 수 있으므로 `화면 숨기기`는 자동으로 해제됩니다.* |
| **크롤링 깊이** | `1단계 (현재)` — 입력한 페이지만 수집합니다.<br>`2단계 (링크)` — 그 페이지의 링크를 따라가 한 단계 더 수집합니다. 시간이 훨씬 오래 걸립니다. |
| **페이지 순회** | 게시판의 `[다음]` 버튼 CSS 선택자(예: `a.next`, `#btnNext`)를 넣으면 1, 2, 3페이지를 넘기며 수집합니다. |
| **최대 순회** | 페이지 순회를 몇 페이지까지 할지 정합니다. (기본 30) |

> ⚠️ **안전장치:** 한 번의 수집에서 방문하는 페이지는 **최대 100개**로 제한됩니다.
> 깊이 2단계에서 링크가 수백 개인 사이트를 만나면 몇 시간 동안 대상 서버에 요청이 계속 나가기 때문입니다.
> 상한에 도달하면 그 사실을 화면에 알리고 정상적으로 마무리합니다. 더 모으려면 시작 주소를 나눠서 여러 번 수집해 주세요.

---

## 📊 3. 결과물 읽는 법

수집이 끝나면 `results/[페이지제목]_[도메인]_[날짜시간]/` 폴더가 생기고, 결과 폴더가 자동으로 열립니다.

| 파일 | 내용 |
|---|---|
| `images/001_이름.jpg` | 수집된 이미지 (번호 순서대로) |
| `images/001_이름.txt` | 그 이미지의 출처 링크·설명·주변 문맥 |
| `checklist.xlsx` | 전체 목록을 한눈에 보는 엑셀 (파일명·문단 제목·설명·주변 문맥·원본 주소·해상도·출처·수집일시) |

**수집 개수가 줄어든 이유도 화면 로그에 표시됩니다.**

```
ℹ️ 7개 저장 / 8개 시도 — 건너뛴 이유: 최소 크기(100x100) 미달 1개
```

이런 식으로 이유가 나오므로, 어떤 설정을 완화해야 하는지 바로 알 수 있습니다.
(주요 이유: `최소 크기 미달`, `허용하지 않는 형식`, `내려받기 실패(네트워크·차단)`, `이미지가 아니거나 파일이 손상됨`)

---

## ⚠️ 4. 법적 고지 및 주의사항 (Disclaimer)

> **[면책 조항]**
> 본 "Gemini 이미지 수집기" 소프트웨어 사용으로 인해 발생하는 **타 플랫폼(네이버, 구글, 인스타그램 등)의 계정 정지, IP 차단, 지적 재산권 침해 분쟁, 금전적 손실 등 모든 문제에 대한 민/형사상 책임은 전적으로 소프트웨어를 임의로 구동한 사용자 본인에게 있습니다.**
> 타인의 저작물과 트래픽 자원을 무단으로 가져오는 동작에 대해 해당 사이트의 `robots.txt` 정책과 이용 약관을 필히 숙지하고 준수하시기 바랍니다.

---

## 💡 5. 문제 해결 (Troubleshooting)

### 수집 결과 관련

*   **Q. "새로 저장된 이미지가 없습니다" 라고만 뜹니다.**
    *   **A.** 같은 사이트를 이전에 수집해서 **이어받기 기록**에 이미 남아 있는 경우입니다. [일반 설정] 탭의 **`이어받기(중복 제외)` 체크를 해제**하고 다시 시도하면 처음부터 다시 받습니다.
    *   그래도 없다면 [필터 및 고급] 탭의 최소 크기·확장자·키워드 조건을 완화해 보세요.

*   **Q. 발견한 개수보다 저장된 개수가 적습니다.**
    *   **A.** 정상입니다. 이미지 크기는 실제로 내려받아야 알 수 있으므로, 받아본 뒤 조건에 맞지 않으면 버립니다. **화면 로그에 `건너뛴 이유` 가 표시**되니 그것을 보고 해당 설정을 조절하세요.

*   **Q. 이미지가 거의 안 잡힙니다.**
    *   **A.** 순서대로 확인해 보세요.
        1. [필터 및 고급] 탭 **최소 크기**를 `0 × 0` 으로 낮춰봅니다.
        2. **허용 확장자**가 모두 해제되어 있지 않은지 확인합니다. (모두 해제 = 아무것도 수집 안 함)
        3. **필수 포함 키워드**를 비웁니다.
        4. **제외 키워드**에서 불필요한 단어를 지웁니다.

*   **Q. AVIF 형식 이미지는 왜 안 받아지나요?**
    *   **A.** 현재 버전은 AVIF를 지원하지 않습니다(이미지 처리 엔진의 기본 구성에 포함되지 않는 형식입니다). 건너뛸 때 화면 로그에 이유가 표시됩니다. 대부분의 사이트는 JPG/PNG/WEBP 대체 이미지를 함께 제공하므로 그쪽이 수집됩니다.

*   **Q. '특정 영역만 수집'을 켰는데 전체가 수집됩니다.**
    *   **A.** CSS 선택자 칸이 비어 있으면 페이지 전체를 수집하며, 이 경우 화면 로그에 안내가 표시됩니다. 선택자를 입력해 주세요. 선택자가 페이지에 없을 때도 전체를 수집하며 로그에 남습니다.

*   **Q. '다음 페이지' 자동 클릭이 동작하지 않습니다.**
    *   **A.** CSS 선택자 칸이 비어 있으면 첫 페이지만 수집하고 로그에 안내가 표시됩니다. 개발자 도구(F12)로 [다음] 버튼의 선택자를 확인해 넣어주세요.

### 실행·접속 관련

*   **Q. 자꾸 '대상 컴퓨터에서 연결을 거부했습니다' 라는 오류가 떠요!**
    *   **A.** 수집하려던 사이트가 "매크로 봇"으로 감지해 튕겨낸 경우가 대부분입니다. [일반 설정] 탭의 `안전 딜레이`를 **높은 단계로 올리고** 다시 시도해 보세요.

*   **Q. 중간에 멈췄는데 지금까지 받은 이미지는 다 날아가나요?**
    *   **A.** 아닙니다. 멈추기 전까지 받아진 이미지와 엑셀 정보는 **결과 폴더**에 안전하게 저장되어 있습니다. 다음에 다시 실행하면 이어받기도 가능합니다.

*   **Q. `[🛑 중지]` 를 눌렀는데 바로 멈추지 않습니다.**
    *   **A.** 진행 중인 요청을 안전하게 정리하는 시간이 필요합니다. 다만 모든 대기 구간이 중지 요청을 즉시 확인하도록 되어 있어 몇 초 안에 멈춥니다.

*   **Q. "이 사이트의 robots.txt 정책이 자동 수집을 허용하지 않습니다" 라고 나옵니다.**
    *   **A.** 사이트 운영자가 자동 수집을 거부한 상태입니다. 원칙적으로 존중해야 하며, 본인 소유 사이트이거나 수집 허가를 받은 경우에만 [일반 설정] 탭에서 해당 옵션을 해제하고 진행하세요.

*   **Q. 로그인이 필요한 사이트의 이미지가 안 받아져요.**
    *   **A.** [접속 및 순회] 탭에서 **수동 로그인**을 켜주세요. 로그인한 브라우저의 인증 정보(쿠키)를 다운로드에도 사용하므로 회원 전용 이미지까지 받을 수 있습니다. (이때 `화면 숨기기`는 자동으로 해제됩니다.)
    *   보안을 위해 **로그인 정보는 해당 사이트에만 전송**되며, 외부 이미지 서버로는 전달되지 않습니다.

*   **Q. 크롬 관련 오류가 뜹니다.**
    *   **A.** 프로그램이 원인별 해결 방법을 창에 직접 안내합니다. 대부분 **크롬 최신 업데이트 후 프로그램 재실행**으로 해결됩니다. 드라이버 버전이 맞지 않으면 자동으로 맞는 버전을 내려받아 복구합니다.

*   **Q. 업데이트했더니 라이센스 키를 다시 입력하라고 합니다.**
    *   **A.** 보안 강화(인증 정보 위조 방지)로 기존 인증 기록을 신뢰할 수 없게 되어 **최초 1회만** 다시 입력이 필요합니다. 구매 시 받으신 같은 키를 그대로 넣으시면 됩니다.

*   **Q. 설정을 바꿨는데 프로그램을 다시 켜면 원래대로 돌아갑니다.**
    *   **A.** 프로그램이 설정 파일을 저장할 권한이 없는 위치에 있을 수 있습니다. 화면 로그에 사유가 표시되니 확인하시고, 프로그램 폴더를 **바탕화면이나 문서 폴더**로 옮겨주세요.

---

## 📞 6. 문의

프로그램 사용 중 문제가 발생하면 `logs/` 폴더의 최신 로그 파일과 함께 문의해 주시면 빠르게 도움드릴 수 있습니다.
