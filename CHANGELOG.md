# 변경 이력 (CHANGELOG)

이 프로그램의 주요 변경 사항을 기록합니다. 형식은 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

---

## [1.0.17] - 2026-08-17 — 바탕화면 바로가기 및 배포 동봉물 수정

v1.0.16 배포 후 **바탕화면 바로가기가 구버전(v1.0.15) exe 를 계속 실행**해서
프로그램 화면에 옛 버전(1.0.15)이 표시되고 아이콘도 깨지던 문제를 수정했습니다.
(원인 분석: `troubleshooting/2026-08-17_바로가기_구버전실행_아이콘깨짐.md`)

> 프로그램 자체에는 문제가 없었습니다. **바로가기가 새 프로그램을 가리키지 않았을 뿐**입니다.
> 수집 동작 코드(`src/`)는 이번 버전에서 변경되지 않았습니다.

### 🔗 수정 — 바로가기

| 항목 | 증상 | 조치 |
|---|---|---|
| 경로 기준 | `$PWD`(실행 시점 현재 폴더) 기준이라, bat 을 다른 폴더에서 호출하거나 정션 경로를 경유하면 엉뚱한 경로가 박힘 | `pushd "%~dp0"` — **bat 자기 위치** 기준으로 변경 |
| 아이콘 깨짐 | 아이콘을 대상 exe 와 **다른 파일**(프로젝트 루트 `app_icon.ico`)에서 가져와, exe 는 살아있는데 아이콘만 사라짐 | 아이콘을 **exe 자체**(`exe,0`)에서 가져옴 — 대상과 아이콘이 같은 파일이라 어긋날 수 없음 |
| 침묵 실패 | exe 가 없어도 깨진 바로가기를 조용히 생성 | 사유를 알리고 **생성하지 않음** + 종료코드 전달 |

### 📦 수정 — 배포 패키지 (고객 영향)

| 항목 | 증상 | 조치 |
|---|---|---|
| 동봉물 누락 | zip 에 **exe 하나만** 들어가, 고객은 바로가기 생성 도구도 매뉴얼도 받지 못했음 (전역수칙 7 위반) | zip 에 `바로가기_만들기.bat` + `MANUAL.md` + `CHANGELOG.md` 동봉 |
| 고객 환경 무조건 실패 | bat 이 프로젝트 루트의 `app_icon.ico` 를 참조 — 고객 PC 에는 없는 파일 | exe 기준으로 바뀌어 고객 환경에서도 정상 동작 |
| 매뉴얼 누락 침묵 | 매뉴얼 파일이 없으면 아무 말 없이 건너뜀 | 없으면 **경고 출력** (매뉴얼 빠진 배포판 방지) |

### 🛠 빌드 스크립트 견고화

- **빌드 실패 침묵:** 실패 시 메시지만 찍고 종료코드 0 으로 끝나 성공처럼 보이던 문제 → `sys.exit(1)`.
- **이름 규칙 단일화:** `Gemini_Image_Crawler_v{버전}` 조립이 4곳에 복사돼 있던 것을
  `app_name()` / `app_dist_dir()` / `app_exe_filename()` 한 벌로 통합.
  (이름 규칙을 바꿀 때 일부만 바뀌어 바로가기가 없는 exe 를 가리키는 사고 방지)

---

## [1.0.16] - 2026-08-17 — 전면 품질 점검 및 수집 손실 버그 대수술

전체 코드 감사(audit)를 통해 발견된 **"조용히 이미지를 놓치는"** 버그 5건과
동작·보안 문제 11건을 한 번에 수정했습니다.

### 🔴 수정 — 수집 결과 손실 (Critical)

| 항목 | 증상 | 원인 | 조치 |
|---|---|---|---|
| 이어받기 기록 | 같은 사이트 재수집 시 아무것도 안 받아짐 | `results/download_history.json` 하나에 **전 사이트**를 몰아 기록 | 사이트별 기록(`results/.history/<도메인>.json`)으로 분리 + **[이어받기 기록 초기화]** 버튼 신설 |
| 수동 로그인 | 로그인해도 회원 전용 이미지가 403 실패 | 셀레니움으로 로그인해 놓고 다운로드는 쿠키 없는 `requests` 로 수행 | 브라우저 세션 쿠키를 다운로더에 전달(`get_session_cookies`) |
| 지연로딩(lazy-load) | 투명 1×1 플레이스홀더만 잔뜩 수집됨 | `src`/`data-src`/`data-original` 3개만 확인 | `srcset`(최대 해상도 선택), `data-lazy-src`, `data-echo` 등 13종 속성 지원 + 플레이스홀더 판별 |
| Base64 이미지 | 페이지 내장(data URI) 이미지가 저장 안 됨 | 파일명이 base64 본문에서 추출돼 **247자** → 윈도우 260자 경로 한계 초과 | 짧은 고유 파일명 생성 (`embed_XXXXXXXX`) |
| 2단계 깊이 크롤링 | 같은 페이지를 수십 번 재방문하며 시간 낭비 | `#앵커` 미제거로 `#a`, `#b` 를 서로 다른 페이지로 인식 | 링크 정규화(프래그먼트 제거·끝슬래시 통일·문서/이미지 링크 제외) |

### 🟠 수정 — 동작 및 신뢰성

- **설정 유실 방지:** 수집 시작 직전에 현재 입력값을 강제 저장. 값을 고치고 바로 [수집 시작]을 눌러도 반영됩니다.
- **진행바:** 실패·중지 시에도 진행바가 멈추도록 수정 (기존에는 성공 시에만 멈춰 영원히 애니메이션).
- **오프라인 라이선스 인증 복구:** 캐시를 Base64로 저장하도록 바뀐 뒤에도 폴백 경로만 평문으로 읽어 항상 실패하던 회귀 수정. 인증창의 기존 키 자동 표시도 복구.
- **수집 중 창 닫기:** 죽은 위젯 접근으로 발생하던 `TclError` 연쇄 제거 (`ui_call` 안전 래퍼 도입).
- **최소 이미지 크기:** 세로(height) 입력칸 추가, 라벨 오타(`최소 오차 크기` → `최소 이미지 크기`) 수정.
- **페이지네이션 순회:** 다음 페이지로 넘기기 **전에** 현재 페이지를 파싱하도록 변경. 기존에는 마지막 페이지 이미지만 남았습니다. 최대 순회 페이지 수(기본 30) 제한 추가.
- **중지 버튼:** 대기 중인 다운로드 작업을 즉시 취소하도록 변경 (기존에는 남은 작업을 모두 기다림).
- **파일 확장자:** 실제 이미지 포맷(PIL 판별) 기준으로 저장. PNG가 `.jpg` 로 저장되던 문제 해결.
- **제외 키워드:** 호스트명은 검사 대상에서 제외. (`banner` 키워드 때문에 `bannershop.co.kr` 이미지가 전멸하던 문제)
- **엑셀 리포트:** 셀당 32,767자 한계를 넘는 Base64 주소를 잘라서 저장 (파일 손상 방지).
- **다운로드 안전장치:** Content-Type 검사 + 용량 상한(기본 20MB) 추가로 메모리 폭주 방지.
- **로그:** 5MB 단위 자동 순환(최대 3개 보관). 쓰기 권한 없는 폴더에 설치돼도 사용자 데이터 폴더로 자동 대체.
- **설정 파일:** 기본값 병합 방식으로 변경. 업데이트로 새 옵션이 추가돼도 기존 `settings.json` 때문에 기능이 꺼지지 않습니다.

### 🔒 보안

- **라이선스 캐시 위조 방지:** HWID 기반 **HMAC-SHA256 서명** 도입. 만료일을 손으로 고친 캐시 파일로 영구 사용하던 경로를 차단합니다.
  > ⚠️ **주의:** 기존 사용자는 업데이트 후 **라이선스 키를 한 번 다시 입력**해야 합니다. (구버전 무서명 캐시는 신뢰할 수 없어 폐기 처리됩니다)
- **미사용 오프라인 라이선스 모듈(`license_manager.py`) 삭제.** 테스트용 키 문자열이 남아 있어 리버싱 시 힌트가 될 수 있었습니다.

### ⚖️ 준법 / UX

- **robots.txt 준수 옵션 추가 (기본 켜짐).** 해제 시 법적 책임을 명시한 경고 창을 띄웁니다.
- **크롬 오류 안내 구체화.** 미설치/버전불일치/권한/네트워크 원인별로 해결 방법을 한국어로 안내합니다.
- 수동 로그인 활성화 시 '화면 숨기기' 자동 해제 안내.
- 메인 화면 하단에 저작권·책임 고지 문구 상시 노출.

### 🛠 빌드 전 최종 점검에서 추가 수정 (2026-08-17)

프로젝트 폴더를 이동한 뒤 재빌드 직전에 전체 점검을 돌려 발견한 항목입니다.

| 항목 | 증상 | 조치 |
|---|---|---|
| 설정 저장 증폭 | `save_settings()` 1회가 `settings.json` 을 **18번** 덮어씀 (슬라이더 조작 시 반복) | `set_many()` 도입 — 18개 항목을 **파일 쓰기 1회**로 저장 |
| 설정 파일 손상 | 원본에 직접 덮어써서, 쓰는 도중 강제 종료되면 JSON 이 반쪽만 남아 설정 전체 초기화 | **임시 파일 → `os.replace`** 원자적 교체 + `fsync` |
| 설정 저장 침묵 실패 | 저장 실패가 로그에만 남아, 사용자는 저장된 줄 알고 계속 사용 | 실패 시 화면 로그에 사유·해결법 노출 (`last_error` + 반환값) |
| 쓰기 권한 없는 폴더 | `config/` 에 쓸 수 없으면 매번 조용히 저장 실패 (주석은 "안전"이라고 잘못 기재) | 로거와 동일하게 **사용자 데이터 폴더로 자동 대체** |
| 확장자 필터 역동작 | 확장자 체크박스를 **모두 해제**하면 필터를 건너뛰어 오히려 전부 수집 | 허용 목록이 비면 '아무것도 허용 안 함'으로 정정 |
| 오프라인 인증 실패 | `ConnectionError` 만 처리해서, 네트워크가 **느릴 때**(ReadTimeout) 캐시 폴백이 동작하지 않아 정상 사용자가 프로그램을 못 켰음 | `RequestException` 으로 확대 + 서버가 JSON 아닌 응답을 줄 때도 폴백 |
| 딜레이 공식 중복 | 요청 간격 계산이 UI 안에만 있어 '표시=동작' 불일치 위험 | `config_manager.delay_bounds()` 단일 함수로 통합 (하한 보정 포함) |
| 경로 조작 방어 취약 | `_safe_hostname` 이 `..` 를 통과시킴 (현재는 `.json` 이 붙어 무해하나 취약) | 연속 점을 `_` 로 치환·앞뒤 점 제거 |

### 🛫 빌드 전 품질점검 게이트 도입 (전역수칙 10)

- `quality-checks.json` + `tools/preflight.py` 설치. 선언한 성질:
  `desktop-exe` / `gui` / `local-json-store` / `multi-thread` / `platform-bot` / `legacy-settings`
- **자동 테스트 114개** 신설 (`tests/`) — 수집 손실 버그 5건 회귀 방지, 설정 원자성·복구,
  옛 저장본 하위호환, 라이선스 캐시 위조·복사 차단, 딜레이 전 구간 전수 검사, 중지 반응성.
- `build.py` 가 **버전 증가·`dist/` 삭제보다 먼저** 게이트를 호출한다.
  실패하면 빌드가 중단되고 버전도 올라가지 않는다.
- 배포판 exe 실행 여부는 **버전 프리픽스**로 검사한다 (exe 이름에 버전이 붙어
  러너 내장 완전일치 검사로는 조용히 통과되는 문제 회피).

### 🧹 정리

- 빌드 잔재 파일 삭제(`build_err.txt`, `build_error.log`, `temp_path*.txt`, 누적된 `.spec` 10개).
- `build.py` 클린 빌드 강화: 모든 `.spec` 및 `__pycache__` 자동 정리.
- 매뉴얼(`MANUAL.md`) 갱신: 신규 옵션 설명 및 문제 해결 항목 5개 추가.

---

## [1.0.15] - 2026-05-12
- 크롬 드라이버 자동 복구(버전 불일치 시 ChromeDriverManager 폴백)
- UI 데이터 바인딩 강화, 로그 정리, Base64 이미지 지원 추가

## [1.0.14] - 2026-03-27
- 보안 패치: 하드코딩 키 제거, 라이선스 암호화, 경로 조작(Path Traversal) 방어

## [1.0.13] 이전
- 초기 상용화: 구글 스프레드시트 라이선스, 이용약관 동의, 기기 바인딩, UI 브랜딩
